# jnote 板書・講義ノート記法

エージェント／著者が `@original/jnote` で板書 PDF を書くときの正本。
API（`theorem` 等）は各バージョンの `README.md` を見ること。ここでは **コンパイルは通るが PDF が崩れる** 落とし穴を優先する。

## 最小テンプレ

```typst
#import "@original/jnote:1.0.1": *
#show: jnote

#text(size: 9pt, fill: gray)[
  板書案 · … \
  流れ: … . 頁数合わせのための圧縮はしない.\
  $u(t,x)$. …
]

= 第 N 回 タイトル

== 前半・板書（約 60 分）

=== 0. 道筋（2 分）

+ 今日やること
+ *やらないこと*

== 後半・演習（約 30 分）

=== 演習 1 — …
```

- 冒頭グレー注記: 流れ／禁則／今日やらないこと。
- 節に所要分を書く。道筋を先に。
- `#remark[ … ]` で範囲外・よくあるミス。

## 数式（Unicode モード）

jnote 板書は **Unicode 数式**（`$∂_t$`, `$μ$`, `$ω$`）を使う。`\partial` や `partial` だけに寄せない（既存板書と揃える）。

| よい | 避ける／注意 |
|------|----------------|
| `$∂_t^2 u = v^2 ∂_x^2 u$` | LaTeX 風 `$\partial_t$` だけの混在 |
| `$1/(2 v) ∫_a^b f(x) dif x$` | 微分形式の $d x$（板書では直立 `$dif x$`） |
| `$exp(upright(i) (k x - ω t))$`（推奨） | `$e^(i(k x - ω t))$`（指数が潰れて $e i(…)$ に見える） |
| `$abs(bold(k))$` またはインライン `$|bold(k)|$`（**表の外**） | 表セル内の `$ω=v\|bold(k)|$`（列が割れる） |
| `$(T ∂_x u)|_(x=ε) - (T ∂_x u)|_(x=-ε)$` | `$[T ∂_x u]_(-ε)^(ε)$` / `$lr([…])_(-ε)^(ε)$`（上下添字が潰れる） |
| `$∂_x (B cos(-k_1 x - ω t))$` | `$∂_x [B cos(…)]$`（`[` がマークアップと衝突しうる） |
| `$0^-$`, `$0^+$` / `wide`（スペース） | `$0-$`（後続と結合）, LaTeX の `$qquad$`（Typst では `wide`） |

### `cases` — 左辺にカンマ区切りを置かない

**悪い（`;` や括弧が壊れる）:**

```typst
$
μ(x),\; T(x)
=
cases(
  μ_1,\; T_1 & (x < 0),
  μ_2,\; T_2 & (x > 0),
)
$
```

**よい:**

```typst
$
(μ(x), T(x))
=
cases(
  (μ_1, T_1) & (x < 0),
  (μ_2, T_2) & (x > 0),
)
$
```

条件付きの単一値は既存どおり:

```typst
$
(u_0^"odd")(x)
:=
cases(
  u_0(x) & (x > 0),
  0 & (x = 0),
  - u_0(-x) & (x < 0),
)
$
```

### 絶対値とノルム

インライン・ブロック数式内の `$|x-y|$` は可（既存板書で使用）。
**Markdown 風パイプ表のセル内**では `|` が列区切りになるので `$abs(x)$` / `$abs(bold(k))$` に置き換える。

## 表は `#table` のみ

Typst は GitHub Flavored Markdown の `| … |` 表をレイアウトしない。
ソースに書くと **パイプが本文にそのまま出る**（コンパイルは成功する）。

**悪い:**

```typst
| | ディリクレ | ノイマン |
|--|-----------|---------|
| 境界条件 | $u(t,0)=0$ | $∂_x u(t,0)=0$ |
```

**よい:**

```typst
#table(
  columns: 3,
  inset: 6pt,
  align: left,
  [], [*ディリクレ*], [*ノイマン*],
  [境界条件], [$u(t,0)=0$], [$∂_x u(t,0)=0$],
  [拡張], [奇], [偶],
)
```

## 構成上の約束（講義板書）

- 前半 ≈ 60 分、後半演習 ≈ 30 分。
- 式展開・検算を省略しない。頁数合わせで圧縮しない。
- 一テーマを完結させてから次へ（途中で行き来しない）。対比表は締めに一度。
- 演習は講義と同じ順。テーマを混ぜない。
- 本文は日本語。説明用に英語単語を残さない（`discontinuous` 等）。

## 見出し番号について

`jnote` は見出しに自動番号を付ける。`= 第 N 回` が `1. 第 N 回` になるのは仕様。
板書タイトルに自分で `1.` を重ね書きしない。

## 検証

```bash
typst compile path/to/note.typ
pdftotext path/to/note.pdf - | head -80
```

`pdftotext` で次を確認する:

- 表がパイプだらけになっていないか
- `$e^(…)$` の指数が本文に潰れていないか
- `cases` の左辺が `;` だらけになっていないか

## 関連

- パッケージ API: 各バージョンの `README.md`
- プレビュー（WSL 9p）: エージェントスキル `tinymist-preview-watch`
- 小テスト（`@original/quiz`）: `typst-form-exam-authoring`（本ファイルとは別系統）
